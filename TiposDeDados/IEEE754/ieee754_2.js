/* Infinity - que pode ser positivo ou negativo, é retornado quando uma operação ultrapassa 
os limites do tipo number*/

1/0;
Math.pow(10,1000); // pow - operação de potenciação
Number.MAX_VALUE * 2; 
-Number.MAX_VALUE * 2;
Math.log(0);